{
  "version": 1,
  "hooks": {
    "preToolUse": [
      {
        "type": "command",
        "powershell": "uv run --script hooks\\render_template.py --protocol copilot; exit 0",
        "bash": "uv run --script hooks/render_template.py --protocol copilot; exit 0",
        "timeoutSec": 60
      }
    ]
  }
}
