[project]
name = "prototype"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    # Duplicated in the PEP 723 header of hooks/render_template.py, which runs
    # with `uv run --script` and therefore does not read this file. Bump both.
    "minijinja>=2.22.0",
]
